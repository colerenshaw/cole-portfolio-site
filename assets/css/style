body { background: #f9f7ef; color: #222; font-family: Arial, sans-serif; margin: 0; }
.navbar { position: sticky; top: 0; background: #fff; border-bottom: 1px solid #ccc; padding: 10px; display: flex; gap: 15px; }
.main-content { padding: 20px; }
.projects-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 20px; }
.photo-gallery { display: grid; grid-template-columns: repeat(auto-fill, minmax(200px, 1fr)); gap: 20px; }
.juxtapose { margin-bottom: 40px; }
.footer { background: #fff; padding: 10px; text-align: center; border-top: 1px solid #ccc; }
